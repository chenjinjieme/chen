<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chen.aigis.dao.CharacterDao">
    <select id="get" resultType="map">
        SELECT u.unit name,
               u.own
        FROM unit u
                 LEFT JOIN sex s ON u.sex = s.id
                 LEFT JOIN rare r ON u.rare = r.id
                 LEFT JOIN class c on u.class = c.id
        WHERE s.sex = #{sex}
          AND r.rare = #{rare}
          AND c.class = #{class}
    </select>
    <insert id="add">
        INSERT INTO unit(id, unit, sex, rare, class)
        VALUES (#{id}, #{name}, #{sex}, #{rare}, #{class})
    </insert>
    <delete id="delete">
        DELETE
        FROM unit
        WHERE id = #{id}
    </delete>
</mapper>